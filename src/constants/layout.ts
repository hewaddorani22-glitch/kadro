/**
 * Height of the tab bar above the device's bottom safe-area inset.
 * The bar itself adds `insets.bottom` on top of this, and scrollable screens
 * reserve the same total so nothing ends up underneath it.
 */
export const TAB_BAR_CONTENT_HEIGHT = 64;
